import provincesFunc from './provinces';
import territoriesFunc from './territories';

export const provinces = provincesFunc;
export const territories = territoriesFunc;

const drc = {
  provinces,
  territories,
};

export default drc;
